# QuickLogin 用户手册

> 适用版本：v3.10.1 · 面向读者：日常使用者
> 技术实现细节见 `docs/PROJECT-STATUS.md` 与 `docs/CODEBASE_OVERVIEW.md`

---

## 一、QuickLogin 是什么

**QuickLogin** 是一个 Chrome 浏览器扩展，让你在**同一个浏览器窗口**里同时登录并使用**同一网站的多个账号**——每个账号拥有自己的标签页、独立的登录状态与存储空间，互不干扰、互不登出。

典型场景：

- 一个管理员账号 + 多个普通业务账号**并行在线**，随时切换焦点；
- 低代码配置平台上，编辑账号 A 的过程中点配置按钮弹出的新页签，**依然是账号 A 的身份**；
- 切换账号不用反复「登出 → 输密码 → 登入」。

对你的体感：**每个标签页是一个完全独立的「分身浏览器」**。

---

## 二、安装

### 2.1 首次安装（手动装载）

1. 拿到发行包：
   - **zip 包**（如 `quicklogin-chrome-v3.10.1.zip`）：先**解压**到一个**固定目录**（Chrome 不能直接装 zip，务必解压，且之后不要移动/删除该目录）；
   - 或直接拿到 **`dist/` 文件夹**（内含 `manifest.json`）；
2. 打开 Chrome，地址栏输入 `chrome://extensions` 回车；
3. 打开右上角的**「开发者模式」**开关；
4. 点左上角**「加载已解压的扩展程序」**，选中解压后的文件夹（含 `manifest.json` 的那一层）；
5. 工具栏出现 QuickLogin 图标（蓝色轮盘）即安装成功。建议点扩展图标右侧的📌把它固定到工具栏。

> Edge 浏览器同样支持：`edge://extensions` → 开发者模式 → 加载解压的扩展。

### 2.2 更新版本（重要！）

1. 用新版本内容**覆盖**原来的文件夹（或解压新版 zip 覆盖）；
2. 到 `chrome://extensions`，在 QuickLogin 卡片上点 **「重新加载」（↻）**。

⚠️ **不点重新加载 = 一直运行旧版本**。Chrome 会缓存扩展的后台脚本，这是「更新后功能没生效」的九成原因。仍不行就「移除」后重新加载 `dist/`（账号数据存在浏览器配置里，移除扩展不会丢失，重新装载后自动回来）。

### 2.3 开发者模式提示

手动装载的扩展，Chrome 每次启动会弹一次「请停用以开发者模式运行的扩展程序」——点**「取消」**即可继续使用，这是手动装载的固有提示，无法根除（正式上架 Chrome 商店后才会消失）。

---

## 三、第一次使用（5 分钟上手）

1. 点工具栏 QuickLogin 图标 → 点**「打开并行管理页」**，进入主工作台；
2. 在页面下方**「站点管理」**输入目标网站的域名（如 `tonbridge-config.aksoegmp.com`，**不含** `https://`）→ 点**「添加并授权」**——浏览器会弹一次权限确认，点允许。**只有授权过的站点才会启用多账号隔离**；
3. 在**「添加并行账号」**表单：选择刚授权的站点、填写**账号名**和**密码**（页签名、盒子可以留空）→ 点**「添加并打开」**；
4. 新标签页会打开目标网站并**自动填表登录**。登录成功后：
   - 账号卡片显示**「在线」**；
   - 标签页标题前面会出现你自定义的**页签名**（没填就用账号名），一眼区分是谁的页签；
5. 重复第 3 步添加更多账号。按 **Alt+Q** 呼出**账号轮盘**，随时秒切身份。

---

## 四、添加账号

### 4.1 一个一个添加（推荐先这样熟悉）

在管理页**「添加并行账号」**表单里逐项填写：

| 字段 | 必填 | 说明 |
|---|---|---|
| **站点** | ✔ | 下拉选择已授权的站点 |
| **盒子** | | 新账号直接归入某个盒子（不选则进「默认盒子」） |
| **页签名** | | 显示在标签页标题前面，便于区分账号；省略则用账号名 |
| **账号名** | ✔ | 登录用户名 |
| **密码** | ✔ | **AES-GCM 加密保存在本机**，不上传任何服务器 |

两个提交按钮的区别：

- **仅保存**：只保存账号，不打开页签（适合先把账号录全，之后再开）；
- **添加并打开**：保存并立即打开新标签页，**自动填表登录**。

### 4.2 批量导入（一次建一批）

1. 先确认**站点下拉**已选中目标站点、**盒子下拉**已选好去向（批量导入会把账号全部建到「当前所选的站点 + 盒子」）；
2. 点表单里的**「批量导入」**展开文本框，支持两种格式：

**格式一：每行一条（逗号或 Tab 分隔）**

```
运营小张,zhangyunying,Passw0rd!
客服小李,lixiaofu,Passw0rd!
审核小王,wangshenhe,Passw0rd!
```

（顺序 = `页签名,账号名,密码`；中文逗号「，」也可以）

**格式二：JSON 数组**

```json
[
  {"tabName": "运营小张", "username": "zhangyunying", "password": "Passw0rd!"},
  {"tabName": "客服小李", "username": "lixiaofu", "password": "Passw0rd!"}
]
```

3. 点**「解析并导入」**——完成后会提示成功/失败条数（缺账号名或密码的行会跳过）。导入的账号都是「仅保存」状态，从列表逐个点「打开」登录即可。

> 提示：批量导入只建账号记录，不会自动打开页签。想「导入即登录」，用 4.3 的备份导入或逐个「添加并打开」。

### 4.3 备份导入 / 导出（换电脑、整机迁移）

管理页**右上角**有两个按钮：

**导出数据**：把当前的**授权站点、全部账号（含加密密码）、盒子配置**打包成一个 JSON 文件下载（文件名如 `quicklogin-backup-2026-08-29.json`）。

**导入数据**：选择一个 QuickLogin 备份文件 → 浏览器弹出**站点授权确认**（一次列出全部站点，点允许）→ 账号自动创建、盒子配置恢复：

- **同站同名的账号会跳过**，不会重复创建；
- 导入的密码在扩展内部用**文件自带的密钥种子解密**，再用**本机的密钥重新加密**入库——全程没有明文密码落盘；
- ⚠️ **备份文件 = 密码本**。文件里带着解密所需的密钥种子，拿到文件就能解出全部密码——请**像对待密码一样保管**，不要发到群里、不要走不可信渠道。

> 换电脑的标准流程：旧机「导出数据」→ 把 json 文件安全传到新机 → 新机装好扩展 → 「导入数据」→ 完成（含站点授权）。

---

## 五、日常使用

### 5.1 打开与聚焦账号

- 账号卡片上点**「打开」**：
  - 账号**已在线** → 直接跳到它现有的标签页（聚焦）；
  - 账号**未在线** → 打开标签页**免密恢复**登录态（不用重新输密码）；
- 点**「新标签页」**：给该账号再开一个同站页签——同一身份可以多页并行。

### 5.2 状态徽标怎么看

账号卡片右上角：

| 徽标 | 含义 |
|---|---|
| `在线 ×N` | 已登录，N = 当前打开的标签页数 |
| `待登录` | 标签页已打开，等自动填表完成 |
| `离线` | 未打开页签（点「打开」即免密恢复） |
| `未授权 · 已暂停` | 站点授权被移除——去「站点管理」重新「添加并授权」即可恢复 |

### 5.3 账号信息修改与删除

- **改页签名**：卡片上直接改，该账号所有标签页标题即时更新；
- **删除**：删除账号记录，并**关闭它的所有并行标签页**（网站账号本身不受影响）。

### 5.4 关于弹窗页签（低代码平台重点）

在已绑定账号的页签里，**站点自己弹出的新页签**（点配置按钮、`window.open`、`target=_blank`）会**自动继承当前账号**——网络身份、存储命名空间、标题角标全部随迁；弹窗里再弹窗也逐级继承。两个例外：

- 手动 **Ctrl+T** 新建的页签不继承（这是留给你有意脱离扩展的口子）；
- `window.open(…, 'noopener')` 弹窗拿不到来源，退化为普通页签（低代码平台极少这样用）。

---

## 六、盒子管理（账号分组）

盒子是账号的分组容器，**轮盘翻页也按盒子进行**。chips 行位于账号列表上方：`全部 / 默认盒子 / 各盒子… / ＋新建盒子`，chip 上的数字 = 该盒账号数；点 chip 筛选列表。

鼠标悬停在盒子 chip 上会出现操作按钮：

| 按钮 | 功能 |
|---|---|
| **✎ 重命名** | 盒内账号**自动随迁**；若输入的名字与已有盒子相同则**并入**那个盒子（会二次确认）。**默认盒子也能改名**：未归盒账号即时显示新名称，不影响任何账号数据；与其它盒子重名会被拒绝 |
| **⏸ / ▶ 禁用 / 启用** | 禁用后**轮盘跳过该盒**（即使盒内有账号），管理页照常可用。禁用的盒子 chip 会变灰并加删除线 |
| **✕ 删除** | 第一步确认删盒；盒内还有账号时第二步选择：**「确定」= 连同账号一并删除**（含其页签），**「取消」= 账号自动移入默认盒子** |

**默认盒子**规则：

- 未归入任何盒子的账号都住在默认盒子；
- **可以重命名**（✎），**不可删除**；
- **为空时自动禁用**——轮盘不显示它，放一个未归盒账号进去即自动恢复。

**＋新建盒子**：chips 行末的虚线按钮，输入名字即创建（空盒子也保留，方便先建架构）。

---

## 七、批量管理

1. 点**「批量管理」**进入批量模式：每张账号卡片出现勾选框；
2. 工具条支持：
   - **移入盒子…**：把勾选的账号批量移入某个盒子——弹窗列出实际存在的盒子，也可**直接输入新盒子名**一步建盒+移入；
   - **删除所选**：连带关闭这些账号的所有页签（会二次确认）；
   - **取消选择**：清空勾选；
3. 再点**「退出批量」**返回。

---

## 八、账号轮盘（快捷切换）

### 8.1 呼出与关闭

- **呼出**：任意页面按 **Alt+Q**（默认快捷键，可在 `chrome://extensions/shortcuts` 改成你顺手的组合）；
- **关闭**：`Esc`、再次按快捷键、或点浮层外的遮罩；
- **形态**：普通网页上直接在页面内展开圆环浮层；受限页面（如浏览器设置页）自动降级为**独立小窗**（注意任务栏）。

### 8.2 选择账号

- 轮盘一圈最多显示 **10 个账号**（按账号专属配色着色，悬停放大高亮）；
- **点击扇区**切换到该账号（打开或聚焦它的页签）；
- **直接按数字键** `1-9`、`0` 快选对应扇区；
- 扇区上的小圆点 = 该账号是否在线（点亮 = 在线）；
- 超过 10 个账号的盒子，Hub 会提示「仅显示前 10 个」，其余账号去管理页打开。

### 8.3 切换盒子

- **滚轮**（在轮盘上滚动）或**点击中心 Hub**：循环切换盒子；
- 中心 Hub 显示：**当前盒名 + 在线/总数**；
- 轮盘**右侧有一条固定的 120° 渐变弧轨道**：每个（未禁用的）盒子对应一个圆点节点，当前盒子的节点**高亮放大并发光，旁边标注盒名**；切换盒子时高亮点**沿轨道平滑滑动**到下一节点（只有一个盒子时节点停在轨道正中）；
- 被禁用的盒子不在轨道上出现；空默认盒自动禁用。

---

## 九、站点管理

- **添加并授权**：输入域名（不含 `https://`）→ 浏览器弹权限确认 → 允许。只有授权过的站点才启用隔离；
- **移除授权**：该站点不再生效隔离，相关账号显示「未授权 · 已暂停」；重新「添加并授权」即恢复，**账号数据不丢**；
- 授权是浏览器级权限，扩展**只在你明确授权过的站点上工作**，其余网站零接触。

---

## 十、数据与安全

- 密码 **AES-GCM 加密存放本机**（IndexedDB），任何服务器（包括扩展作者）都拿不到；
- 登录态快照保存在浏览器**会话级存储**，关闭浏览器即清空（下次打开免密恢复依赖它，重启浏览器后首次打开会走一次静默登录/自动填表）；
- 扩展只在**你明确授权过的站点**生效；
- **备份文件等效密码本**（内含解密种子），务必像密码一样保管；
- 安全边界声明：扩展保证的是**客户端呈现层的账号分离**；若平台服务端本身存在越权缺陷（如某账号能读到他人数据），应向平台方反馈。

---

## 十一、快捷键速查

| 按键 | 场景 | 作用 |
|---|---|---|
| **Alt+Q** | 任意页面 | 呼出/关闭账号轮盘 |
| **1-9、0** | 轮盘展开时 | 直选对应账号 |
| **Esc** | 轮盘展开时 | 关闭轮盘 |
| **滚轮 / 点击 Hub** | 轮盘展开时 | 切换盒子 |

> 快捷键冲突/失灵：到 `chrome://extensions/shortcuts` 查看并重设。

---

## 十二、常见问题（FAQ）

**Q1：明明是最新版本，功能却没生效？**
九成是没点**「重新加载」**。到 `chrome://extensions` 点扩展卡片上的 ↻；仍不行就「移除」后重新加载 `dist/`（账号数据不会丢）。

**Q2：能同时打开同一个账号的多个标签页吗？**
可以。卡片上点「新标签页」，同一身份多页并行，标题角标相同。

**Q3：站点自己弹出的新页签（配置按钮/编辑弹窗）会保持账号身份吗？**
**会**——自动继承当前账号，弹窗套弹窗也逐级继承。例外：手动 Ctrl+T 的页签不继承；`noopener` 弹窗无法识别来源。弹窗跳到未授权的其它域名时，扩展会自动解除继承以防身份外漏。

**Q4：在一个页签里退出账号，其它账号会跟着退出吗？**
**不会**（v3.10.0 起）。账号间的广播通道已按账号隔离。若仍遇同步登出，多半是站点服务端强制「单设备登录」——后登录的顶掉先登录的，这属于站点侧行为，扩展无法对抗。

**Q4b：我想用一个「普通」页签手动登录一个新账号（不通过扩展添加），会影响快速登录的账号吗？**
**不会**（v3.10.2 起）。扩展账号的会话 Cookie 不再残留在浏览器的公共 Cookie 区——普通页签从匿名状态开始，手动登录新账号不会被站点误认为「替换旧会话」，快速登录的账号安然无恙。两种登录方式各走各的通道，互不干扰；普通页签登录的账号想纳管，随时在管理页「添加并行账号」录入即可。

**Q4c：从账号 A 的页面新开的页签（或平台弹窗）里，我想登录另一个账号 B，可以吗？**
可以（v3.10.3 起）。这类页签默认继承账号 A（方便平台配置弹窗保持身份）；一旦你在里面**登录了另一个账号**，扩展会识别出「这个页签被你用作原始浏览器了」——自动把它**转回普通页签**（B 的登录在原生状态下完成，不受扩展干扰），同时**回滚它对账号 A 数据的全部写入**，A 的其它页签与数据零污染。关掉该页签或继续用都可以，A 不受影响。

**Q5：删除盒子会删掉账号吗？**
由你决定：删盒时若盒内还有账号，会问你「**一并删除**」还是「**移入默认盒子**」。不选直接取消 = 只删空盒。

**Q6：轮盘按 Alt+Q 没反应？**
看该页面是否为受限页（扩展会自动改弹独立小窗，注意任务栏）；或到 `chrome://extensions/shortcuts` 检查/重设快捷键。

**Q7：换电脑数据还在吗？**
账号与加密密码存在**本机浏览器配置**里，不随浏览器账号同步。换机请走「导出数据 → 新机导入数据」（见 4.3）。

**Q8：导入备份后账号显示「未授权 · 已暂停」？**
说明站点授权没有完成。去「站点管理」对对应域名重新「添加并授权」即可恢复，账号数据不受影响（v3.10.1 起导入时若授权失败会明确提示）。

---

## 十三、已知边界

- 站点若在 **Web Worker** 内打开 IndexedDB，该部分不受隔离补丁覆盖（罕见路径）；
- Cookie 快照在**登录时点**采集一次；站点若在会话中途轮换会话标识，该账号需重新登录一次以刷新快照（令牌本身不受影响）；
- 未授权站点的轮盘浮层自动降级为独立小窗；
- `window.open(…, 'noopener')` 弹窗无法自动继承账号身份；
- 轮盘单盒最多展示 10 个账号，超出部分请用管理页。

---

*项目文档索引见根目录 `README.md`；架构与实现细节见 `docs/PROJECT-STATUS.md`、`docs/CODEBASE_OVERVIEW.md`。*
