{
  "name": "sessionbox-reborn",
  "version": "2.0.0",
  "private": true,
  "description": "SessionBox 重构项目：单浏览器多账号登录 + 标签页标题改写为账号名",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  },
  "allowScripts": {
    "esbuild@0.24.2": true
  }
}
